{
  "name": "surpay-sdk-ts",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "clean": "rm -rf packages/*/dist",
    "changeset": "bunx changeset",
    "changeset:version": "bunx changeset version",
    "changeset:release": "bunx changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0"
  }
}
